# /tts

generates the wav file for given text

required input
body: {
text: `your text here`
}

# /viseme

returns the list of visemeId and offset for given text

required input
body: {
text: `your text here`
}
